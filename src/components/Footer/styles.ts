import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  boxMainContent: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
});

export default useStyles;
